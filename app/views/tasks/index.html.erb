<h1>My Tasks</h1>

<%= link_to "Add a new task", tasks_new_path %>

<ul>

  <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task) %> [ <%= task.completed ? "X" : "" %> ] </li>
  <% end %>

</ul>
